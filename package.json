{
  "name": "api-extraescolares",
  "version": "0.1.0",
  "description": "API REST para las actividades extraescolares",
  "main": "src/index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node src/index.js",
    "dev": "cross-env NODE_ENV=development node --watch src/index.js",
    "test": "cross-env NODE_ENV=test jest --verbose --silent --detectOpenHandles",
    "test:watch": "npm run test -- --watch"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "express": "^4.21.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
